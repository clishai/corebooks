# Superpowers brainstorm sessions (local only, never commit)
.superpowers/

# Git worktrees (local only, never commit)
.worktrees/

# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# SQLite database files (live user data — never commit)
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs
*.log
npm-debug.log*

# macOS system files
.DS_Store

# Test coverage output
coverage/

# TypeScript build info
*.tsbuildinfo
/src/generated/prisma
